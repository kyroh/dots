#!/bin/bash
# Replace 'your_terminal' with the terminal you use (e.g., alacritty, kitty, etc.)
# Replace 'your_venv_path' with the path to your virtual environment
kitty --command "bash -c 'beta; exec bash'"

